name: message_board_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # iOS style icons
  cupertino_icons: ^1.0.8

  # Firebase core (required)
  firebase_core: ^3.0.0

  # Firebase authentication
  firebase_auth: ^5.0.0

  # Cloud Firestore database
  cloud_firestore: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # To add assets later, uncomment and add files:
  # assets:
  #   - assets/
